<?xml version="1.0" encoding="utf-8"?>
<!--
  Descent 3 - Meta Quest port.

  Built in two flavours by quest/build_apk.sh:
    APK_MODE=vr   (default) immersive OpenXR app
    APK_MODE=flat           2D panel app; the VR-BEGIN/VR-END sections below are
                            removed and D3VR_ENABLED is set to false, which makes
                            D3Activity start the engine with -novr.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.descentdevelopers.descent3"
    android:versionCode="1"
    android:versionName="1.6.0-quest">

    <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="32" />

    <uses-feature android:glEsVersion="0x00030002" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- VR-BEGIN -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" android:version="1" />
    <!-- Optional hand tracking: without it Horizon refuses to launch the app while
         the controllers are idle. The game itself is played with the controllers. -->
    <uses-feature android:name="oculus.software.handtracking" android:required="false" />
    <!-- The system keyboard as a VR overlay, for text fields such as the pilot name. -->
    <uses-feature android:name="oculus.software.overlay_keyboard" android:required="false" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <!-- OpenXR loader: permissions and runtime broker lookup (from the loader AAR's manifest). -->
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR_SYSTEM" />
    <queries>
        <provider android:authorities="org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker" />
        <intent>
            <action android:name="org.khronos.openxr.OpenXRRuntimeService" />
        </intent>
        <intent>
            <action android:name="org.khronos.openxr.OpenXRApiLayerService" />
        </intent>
    </queries>
    <!-- VR-END -->

    <application
        android:label="Descent 3"
        android:allowBackup="false"
        android:hasCode="true">

        <meta-data android:name="SDL_ENV.SDL_ANDROID_TRAP_BACK_BUTTON" android:value="0" />
        <meta-data android:name="D3VR_ENABLED" android:value="D3VR_ENABLED_VALUE" />

        <!-- VR-BEGIN -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest2|questpro|quest3|quest3s" />
        <!-- VR-END -->

        <activity
            android:name=".D3Activity"
            android:label="Descent 3"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:resizeableActivity="false"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:configChanges="density|orientation|keyboardHidden|keyboard|navigation|screenSize|screenLayout|uiMode|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- VR-BEGIN -->
                <category android:name="com.oculus.intent.category.VR" />
                <!-- VR-END -->
            </intent-filter>
        </activity>
    </application>
</manifest>
